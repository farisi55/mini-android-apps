<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBasic"
    tools:context="com.example.banus.cookies.MainActivity">

    <!--
    <ImageView
        android:id="@+id/img_after"
        android:layout_width="match_parent"
        android:layout_height="400dp"
        android:layout_weight="1"
        android:scaleType="fitXY"
        android:src="@drawable/before_cookie" />

    <TextView
        android:id="@+id/text_after"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Saya Lapar"
        android:layout_below="@id/img_after"
        android:textSize="10pt"
        android:textStyle="bold"
        android:textColor="@color/colorText"
        android:layout_margin="10dp"/>

    <Button
        android:id="@+id/button_after"
        android:layout_width="150dp"
        android:layout_height="50dp"
        android:layout_below="@id/text_after"
        android:text="Puasa Kue"
        android:layout_margin="10dp"
        android:onClick="puasaCookie"/>
        >
    -->

    <!-- before -->
    <ImageView
        android:id="@+id/img_before"
        android:layout_width="match_parent"
        android:layout_height="400dp"
        android:layout_weight="1"
        android:scaleType="fitXY"
        android:src="@drawable/before_cookie" />

    <TextView
        android:id="@+id/text_before"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Saya Lapar"
        android:layout_below="@id/img_before"
        android:textSize="10pt"
        android:textStyle="bold"
        android:textColor="@color/colorText"
        android:layout_margin="10dp"/>

    <Button
        android:id="@+id/button_before"
        android:layout_width="150dp"
        android:layout_height="50dp"
        android:layout_below="@id/text_before"
        android:text="Makan Kue"
        android:layout_margin="10dp"
        android:onClick="eatCookie"/>

</RelativeLayout>
